{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "lldb",
            "request": "launch",
            "windows": {
                "program": "${workspaceFolder}/build/Turbo.exe",
            },
            "linux": {
                //"program": "${workspaceFolder}/build/samples/PureHelloTriangle",
                "program": "${workspaceFolder}/build/Turbo",
            },
            "args": [
                "--arch x86_64"
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "build",
        }
    ]
}