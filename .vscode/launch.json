{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Turbo.exe",
            "args": [
                "--arch x86_64"
            ],
            "cwd": "${fileDirname}",
            "preLaunchTask": "build",
        }
    ]
}